// Fill in your own Firebase project credentials below.
// You can either edit this file directly and reload the page,
// or leave it blank and paste the values into the form on the page instead.
//
// Find these values in the Firebase Console:
// Project settings > General > Your apps > SDK setup and configuration

window.firebaseConfig = {
  apiKey: "",
  databaseURL: "",
  projectId: ""
};

// The path in your Realtime Database where sensor readings are stored,
// e.g. if your data lives under /plant_data/{timestamp}, use "plant_data".
window.firebaseDataPath = "plant_data";
